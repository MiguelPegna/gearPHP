<?php
    
    require_once '../core/autoload.php';
    require_once '../routes/web.php';


    

    

    /* Leer el archivo .env
    $envFile = file('../.env');

    // Crear un array para almacenar las variables de entorno
    $env = [];

    // Recorrer las líneas del archivo .env y almacenar las variables en el array
    foreach ($envFile as $line) {
        list($key, $value) = explode('=', $line);
        $envVariables[trim($key)] = trim($value);
    }

    // Acceder a las variables de entorno
    $dbHost = $envFile['DB_HOST'];
    $dbUser = $env['DB_USER'];
    $dbPass = $env['DB_PASS'];
    $dbName = $env['DB_NAME'];

// Utilizar las variables de entorno en tu aplicación
echo "DB_HOST: $dbHost, DB_USER: $dbUser, DB_PASS: $dbPass, DB_NAME: $dbName";
*/